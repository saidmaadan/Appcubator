<header id="content-header">
  <h1>Checkins for <%= link_to(@idea.title, @idea) %></h1>
</header>

<ol>
  <% @checkins.each do |checkin| %>
    <li>
      <p>
      <%= profile_image_for(checkin.user, size: 35) %>
      <%= checkin.skills %>

      </p>
      <p>
        <%= checkin.comment %>
      </p>
    </li>
  <% end %>
</ol>
