<div class='container'>
<div id="ideas">
<% if @ideas.any? %>
<% @ideas.in_groups_of(3) do |group| %>
<div class='row'>
<% group.each do |idea| %>
<% if idea %>
<div class='col-md-4'>
<h2><%= link_to(idea.title, idea) %></h2>
<p> <%= truncate(idea.description, length: 150, separator: ' ') %></p>
<!-- <table>
          <tr>
            <th>Goal</th>
            <td><%= idea.goal %></td>
          </tr>
          <tr>
            <th>Market Potential</th>
            <td><%= idea.market %></td>
          </tr>
          <tr>
            <th>category</th>
            <td><%= idea.category %></td>
          </tr>
        </table> -->

      </article>
      </div>
      <% end %>
      <% end %>
      </div>
      <% end %>
      <% end %>
      </div>
      </div>

