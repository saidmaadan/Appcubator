<div class='container'>
<div class='row'>
<div class='col-md-4'></div>
<div class='col-md-4'>
<h3>Startup and Project-Ideas</h3></div>
<div class='col-md-4'></div>
<div class='col-md-12 idea_place'><br>
<div class='col-md-9 idea'>
<% if @ideas.any? %>
<% @ideas.in_groups_of(2) do |group| %>
<% group.each do |idea| %>
<% if idea %>
<div class='col-md-6'>
<h3><%= link_to(idea.title, idea) %></h3>
<p> <%= truncate(idea.description, length: 150, separator: ' ') %></p>
<!-- <table>
          <tr>
            <th>Goal</th>
            <td><%= idea.goal %></td>
          </tr>
          <tr>
            <th>Market Potential</th>
            <td><%= idea.market %></td>
          </tr>
          <tr>
            <th>category</th>
            <td><%= idea.category %></td>
          </tr>
        </table> -->
      
      </div>
      <% end %>
      <% end %>
      <% end %>
      <% end %>
     <div class='paginate'>
    <%= paginate @ideas, :outer_window => 3 %>
</div>
      </div>
       <div class='col-md-1'></div>
      <div class='col-md-2 well'>
        <%= link_to ' Post Startup Idea', new_idea_path, class: 'fa fa-folder-open-o' %><br><br>
        <%= link_to ' Add Project', new_project_path, class: 'fa fa-folder-open' %>
      </div>
      </div>
      </div>
      </div>

