<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="msvalidate.01" content="DA89893F2AE9176311128B79AC667BCD" />
    <title><%= content_for?(:title) ? yield(:title) : "Appcubator" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Appcubator" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>

    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <section>
       <%= render 'layouts/navigation_nav' %>
    </section>
    <main role="main">
       <%= render 'layouts/messages' %>
       <%= yield %>
       <div class="push"></div>

    </main>
    <%= render 'layouts/footer' %>
  </body>

</html>
