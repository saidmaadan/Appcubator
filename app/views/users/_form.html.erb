
<%= form_for(@user) do |f| %>
  <%= render "shared/errors", object: @user %>
  <fieldset>

      <p class="required">


        <%= f.text_field :name, size: 40, autofocus: true, placeholder: "Full name" %>
      </p>
      <p class="required">

          <%= f.text_field :username, size: 40,
                    placeholder: "Username Alphanumeric characters only!" %>
</p>
      <p class="required">

        <%= f.email_field :email, size: 40, placeholder: "Email Address" %>
      </p>
      <p class="required">

        <%= f.password_field :password, size: 40, placeholder: "Password 8 or more characters" %></p>

      <p class="required">
        <%= f.password_field :password_confirmation, size: 40, placeholder: "Confirm Password" %></p>

    <p>
      <% if @user.new_record? %>
        <%= f.submit "Create Account" %>
      <% else %>
        <%= f.submit "Update Account" %>
      <% end %>
    </p>
  </fieldset>
<% end %>
